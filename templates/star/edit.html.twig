{% extends 'base.html.twig' %}

{% block title %}Modification de l'étoile{% endblock %}

{% block main %}
    <h1>Modification de l'étoile</h1>

    <table class="table">

    {{ include('star/_form.html.twig', {'button_label': 'Update'}) }}&nbsp

    <a href="{{ path('app_star_index') }}" class="btn btn-secondary btn-sm" role="button">Revenir à la liste</a>&nbsp

    {{ include('star/_delete_form.html.twig') }}
    </table>
{% endblock %}
